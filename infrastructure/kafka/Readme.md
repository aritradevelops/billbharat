# if having trouble with permission on the volume
# then run the following command
```bash 
sudo chown -R 1000:1000 ./data # make the user 1000:1000 the owner of the data directory.
sudo chmod -R 755 ./data # give read, write and execute permissions for the owner.
```
